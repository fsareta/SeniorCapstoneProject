@model IEnumerable<Level>
<div class=" row">
	<div class="col-sm-2">
<form asp-action="Index" method="get">
		</form>
			</div>
<div class="col-sm-2">
	<form asp-action="Index" method="get">
	</form></div></div>
<table class="table">
	<tr>
<th> Level </th>

	</tr>
	@*Make a row for each album*@
	@foreach (Level l in Model)
	{	<tr>
	<td>@l.Id</td>
	
	</tr>
	}

</table>
<div class="form-group">
		<a button type="button" class="btn btn-outline-primary" asp-action="Create">Create New Level</a>
	</div>